# Perplexity Space — Tech Projects / Tweaks / Fixes / Troubleshooting

> Space-level instructions. These extend and override `perplexity/account-wide.md` within this Space.
> Last updated: 31.03.26

---

## Space Context
- Scope: Tech projects, dev work, system tweaks, troubleshooting, fixes
- Applies to: All threads within this Perplexity Space
- Overrides: Account-wide instructions still apply unless contradicted here

---

## Additional Title Shortcuts (Space-only)

| Shortcut | Action |
|---|---|
| `ra` | Rename all — update title + all emoji |
| `ua` | Update all titles in thread |
| `td` | Title with date prefix |
| `tda` | Title with date prefix + all emoji update |

---

## Space-Specific Shortcuts

| Shortcut | Action |
|---|---|
| `g.resume` | Generate resume asset |
| `g.cover` | Generate cover letter |
| `g.pack` | Bundle all assets |
| `g.confirm` | Confirm generation plan |
| `z.confirm` | Confirm zip/export plan |
| `zip.full` | Full zip export |

---

## Extended Export Commands (Space-only)

| Command | Action |
|---|---|
| `/export weekly` | Weekly summary export |
| `/export daily a..b` | Daily export for date range |
| `/export range` | Custom range export |
| `/export resume` | Resume-specific export |

---

## Safe Mode Export (4-step)
1. List all files to be generated
2. Confirm plan before generating
3. Generate file by file, validate each
4. Bundle only after all files validated

> `recover` → rebuild any missing or failed files from last export

---

## Notes
- These shortcuts are Space-scoped and should not bleed into other Spaces or account-wide threads
- Account-wide `[auto-r]` title logic still applies here
